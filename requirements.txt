aiohttp==3.13.3
anthropic==0.84.0
assemblyai==0.56.0
boto3
deepgram-sdk==3.11.0
fal_client==0.13.1
fastapi==0.135.1
google-cloud-speech
httpx==0.28.1
imageio-ffmpeg==0.6.0
lumaai==1.20.1
openai==2.28.0
pydub==0.25.1
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-mock==3.15.1
python-dotenv==1.2.2
python-multipart==0.0.22
replicate==1.0.7
requests==2.32.5
rev_ai==2.21.0
# For GPU acceleration, install all requirements first, then reinstall torch with CUDA:
#   pip install -r requirements.txt
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121 --force-reinstall --no-deps
# setup.bat / setup.sh do this automatically when an NVIDIA GPU is detected.
torch
uvicorn==0.42.0
whisperx
# pyannote.audio version is intentionally unpinned: whisperx 3.3+ requires >=3.3.2,
# which is incompatible with the old <3.3.0 upper bound. Let pip resolve the version.
pyannote.audio
